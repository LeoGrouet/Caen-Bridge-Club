const buttonAdmin = document.getElementById("logAdmin");
buttonAdmin.addEventListener("click", () => {
  // popUp de register
  // if login + MDP match members login + MDP alors
  console.log("coucou");
  window.location.href = "/admin";
  // Else
  //erreur
  //res.redirect
});
